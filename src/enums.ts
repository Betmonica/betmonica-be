export enum BET_STATUSES {
	UPCOMING = 'upcoming',
	CANCELLED = 'cancelled',
	WON = 'won',
	LOST = 'lost'
}

export enum MATCH_STATUSES {
	UPCOMING = 'upcoming',
	CANCELLED = 'cancelled',
	FINISHED = 'finished'
}
